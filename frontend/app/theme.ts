import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
        primary: {
            main: HSL(232, 61, 72),
            // Hover color
            dark: HSL(232, 61, 60)
        },
        secondary: {
            main: HSL(52, 61, 72);
            // Hover color
            dark: HSL(52, 61, 60);
        }
        background: {
            default: HSL(240, 18, 98),
            paper: HSL(240, 18, 94),
        }
        text: {
            primary:
            secondary: 
        }
    }
})